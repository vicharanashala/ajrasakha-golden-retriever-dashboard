{
  "name": "golden-retrieval-tester",
  "version": "1.0.0",
  "description": "Dashboard for comparing Golden Database retrieval API versions.",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "start": "npm run preview --workspace client",
    "test": "npm run test --workspace client",
    "typecheck": "npm run typecheck --workspace client"
  },
  "engines": {
    "node": ">=20"
  }
}
